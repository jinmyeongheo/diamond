tomcat -> httpReq,httpRes -> dispatcherServlet -> getHandler(컨트롤러)
-> getHandlerAdapter -> adpater를 이용해서 handler 실행전 -> (handler)inertceptor 실행 applyPreHandle 
-> 모든 interceptor 통과 -> handlerAdpater로 handle() 메소드 실행 ->
handle을 실행하기 위한 파리미터를 생성위해 argumentResolver실행
-> 어떤 argumentResolver를 실행할지 파라미터 앞의 어노테이션을 보고 판단
-> 일치하는 어노테이션이면 선택한 argumentResolver실행 -> 파리미터 매핑 -> controller 호출 ->
postHandle 인터셉터 실행 -> result 응답

서블릿(Servlet)은 클라이언트 요청을 처리하고, 그 결과를 반환하는 웹 프로그래밍 기술
스프링mvc에서는 http 메세지를 읽고 응답을 만들어주는 역할을 하며 개발자는 비즈니스 로직에만 집중
톰캣(서블릿 컨테이너) 서블릿 객체를 생성, 초기화, 호출, 종료하는 생명주기 관리 싱글톤
동시 요청을 위한 멀티 쓰레드 처리 지원

argumentResolver 컨트롤러에서 요청 데이터에 대해 공통으로 처리해야하는 로직이 있을 때 사용하면 좋음.
전략인터페이스, 요청데이터 앞의 어노테이션을 보고 바인딩여부를 결정함.
어노테이션 커스텀해서 파리미터 바인딩할 수 있음. 인증, 인가 등..

생성자주입을 권장하는 이유. 필드의 불변성, 순환참조방지, 싱글톤,


http server vs tomcat
tomcat은 Catalina라는 서블릿 컨테이너와 자체 HTTP 서버를 내장하고 있음.
Java 가상 머신(JVM) 설정에 따라 mod_proxy 및 mod_jk와 같은 전통적인 Apache 커넥터를 사용하는 것보다 더 나은 성능을 낼 수 있습니다.
tomcat ( catalina(servlet container) + coyote(http component) + jsp)


tomcat outputstream 차면 flush 해줌.

jdbc가 하는것
tcp/ip소캣통신
db 프로토콜 준수
타입변환 java -> db
궁금하면 보자
https://dev.mysql.com/doc/connector-j/en/


JDBC 기능 중 직접 구현할 만한 것:
Connection Pool 직접 설계 및 구현.(커넥션 관리)
Statement와 PreparedStatement의 차이를 구현해 보여주는 샘플 코드 작성.
SQL 실행 및 쿼리 파싱.
트랜잭션 처리.
데이터베이스 예외 처리


## 역할
- 당신은 {{subject}} 분야의 전문가입니다.
- 당신의 목표는 {{target}}이라는 개념을 초보자가 쉽게 이해할 수 있도록 사례나 예시 또는 이야기를 통해 아주 쉽게 설명하는 것입니다.
## 지침
- 친절하고 대화체로 말해주세요.
- 복잡한 용어는 피하고, 꼭 필요한 경우에는 간단히 정의를 덧붙이세요.
---
## 역할
- 당신은 교육 전문가이며, 서술형 질문 개발과 평가에 능숙합니다.
- 사용자의 학습 향상을 돕는 것이 목표입니다.
## 지침
1. 사용자가 입력한 주제와 난이도에 따라 기본 서술형 질문 3개를 생성하세요.
2. 사용자가 답변하기 전까지는 정답을 말하지 마세요.
3. 사용자가 답변을 제출하면 점수를 매기고 평가 및 피드백을 제공하세요.
## 입력
주제 : {{subject}}
난이도 : 중등/고등/ 대학/대학원
---
{{target}}라는 개념을 더 확장해서 학습하고 싶어.

그에 필요한 질문을 다양한 관점에서 던져주고
답까지 자세하게 알려줘.
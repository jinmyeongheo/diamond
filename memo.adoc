더미데이터

DELIMITER $$

CREATE PROCEDURE insert_dummy_data(IN total_rows INT)
BEGIN
    DECLARE i INT DEFAULT 1;

    -- 루프를 사용하여 반복적으로 데이터를 삽입
    WHILE i <= total_rows DO
            INSERT INTO sample (id, Author_name, country)
            VALUES (i, CONCAT('Name', i), CONCAT('country',i)); -- 이름은 'Name' + i, 나이는 20~50 사이의 임의 값
            SET i = i + 1;
        END WHILE;

END$$

DELIMITER ;

CALL insert_dummy_data(10000);
DROP PROCEDURE IF EXISTS insert_dummy_data;

https://generatedata.com/
https://corinprodo.tistory.com/19
https://jojoldu.tistory.com/528?category=637935

페이징
limit offset 페이징
    offset만큼 데이터를 읽어와서 뿌리기때문에
    페이징 처리시 앞에 읽었던 데이터를 다시읽어야함.
    offset의 값이 커질 수록 성능이 저하됨.
no-limit == cursor기반 페이징
    where에 사용되는 칼럼에 index 적용.
    Cursor 기반 페이징
    Cursor 기반 페이징 방식은 조회 시작 부분을 인덱스로 빠르게 찾아 매번 첫 페이지만 읽도록 하는 방식이다.
    아무리 페이지가 뒤로 가더라도 처음 페이지를 읽은 것과 동일한 성능을 가지게 되므로, Offset 방식보다 훨씬 성능이 개선된다.
    그러나, 이 방식은 원래 페이지 번호가 없고, 다음 페이지를 호출할 수 있는 키워드 등을 제공하는 페이징 기능이다. 아래와 같은 시스템 (흔히 SNS 게시물 조회 API에서 많이 볼 수 있다) 에서 사용한다.
JOIN 을 활용한 페이징

https://velog.io/@claraqn/%EA%B3%A0%EB%AF%BC-%EC%BB%AC%EB%9F%BC-%EC%88%98%EC%97%90-%EB%94%B0%EB%A5%B8-%EC%84%B1%EB%8A%A5-%EC%B0%A8%EC%9D%B4
https://gimquokka.github.io/spring%20data/Querydsl%EC%9D%84-%ED%99%9C%EC%9A%A9%ED%95%9C-%EB%B3%B5%EC%9E%A1%ED%95%9C-%EB%8F%99%EC%A0%81%EC%BF%BC%EB%A6%AC-%EA%B5%AC%ED%98%84%EA%B8%B0/
운체
https://blockdmask.tistory.com/69



-- User 테이블 (사용자 정보)
CREATE TABLE User (
                      id INT AUTO_INCREMENT PRIMARY KEY,
                      username VARCHAR(255) NOT NULL,
                      password VARCHAR(255) NOT NULL,
                      email VARCHAR(255) NOT NULL,
                      phone VARCHAR(20),
                      role ENUM('CUSTOMER', 'SELLER', 'ADMIN') NOT NULL,
                      created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- Category 테이블 (카테고리 정보)
CREATE TABLE Category (
                          id INT AUTO_INCREMENT PRIMARY KEY,
                          name VARCHAR(255) NOT NULL,
                          description TEXT
);

-- Product 테이블 (상품 정보)
CREATE TABLE Product (
                         id INT AUTO_INCREMENT PRIMARY KEY,
                         name VARCHAR(255) NOT NULL,
                         description TEXT,
                         price DECIMAL(10, 2) NOT NULL,
                         stock INT NOT NULL,
                         category_id INT,
                         created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
                         updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
                         FOREIGN KEY (category_id) REFERENCES Category(id)
);



-- Order 테이블 (주문 정보)
CREATE TABLE Order_Info (
                       id INT AUTO_INCREMENT PRIMARY KEY,
                       user_id INT,
                       order_date TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
                       total_amount DECIMAL(10, 2) NOT NULL,
                       status ENUM('PENDING', 'COMPLETED', 'SHIPPING', 'DELIVERED', 'CANCELED') NOT NULL,
                       payment_method VARCHAR(50),
                       FOREIGN KEY (user_id) REFERENCES User(id)
);

-- Order_Item 테이블 (주문 상품 정보)
CREATE TABLE Order_Item (
                            id INT AUTO_INCREMENT PRIMARY KEY,
                            order_id INT,
                            product_id INT,
                            quantity INT NOT NULL,
                            price DECIMAL(10, 2) NOT NULL,
                            total_price DECIMAL(10, 2) NOT NULL,
                            FOREIGN KEY (order_id) REFERENCES Order_Info(id),
                            FOREIGN KEY (product_id) REFERENCES Product(id)
);

-- Shopping_Cart 테이블 (장바구니 정보)
CREATE TABLE Shopping_Cart (
                               id INT AUTO_INCREMENT PRIMARY KEY,
                               user_id INT,
                               product_id INT,
                               quantity INT NOT NULL,
                               added_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
                               FOREIGN KEY (user_id) REFERENCES User(id),
                               FOREIGN KEY (product_id) REFERENCES Product(id)
);

-- Payment 테이블 (결제 정보)
CREATE TABLE Payment (
                         id INT AUTO_INCREMENT PRIMARY KEY,
                         order_id INT,
                         payment_date TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
                         amount DECIMAL(10, 2) NOT NULL,
                         payment_method VARCHAR(50),
                         status ENUM('SUCCESS', 'FAILED', 'PENDING') NOT NULL,
                         FOREIGN KEY (order_id) REFERENCES Order_Info(id)
);

-- Address 테이블 (사용자 주소 정보)
CREATE TABLE Address (
                         id INT AUTO_INCREMENT PRIMARY KEY,
                         user_id INT,
                         recipient_name VARCHAR(255) NOT NULL,
                         address_line1 VARCHAR(255) NOT NULL,
                         address_line2 VARCHAR(255),
                         city VARCHAR(100) NOT NULL,
                         postal_code VARCHAR(20) NOT NULL,
                         phone VARCHAR(20) NOT NULL,
                         FOREIGN KEY (user_id) REFERENCES User(id)
);

-- Review 테이블 (리뷰 정보)
CREATE TABLE Review (
                        id INT AUTO_INCREMENT PRIMARY KEY,
                        user_id INT,
                        product_id INT,
                        rating INT CHECK (rating BETWEEN 1 AND 5),
                        comment TEXT,
                        created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
                        FOREIGN KEY (user_id) REFERENCES User(id),
                        FOREIGN KEY (product_id) REFERENCES Product(id)
);

-- Wishlist 테이블 (위시리스트 정보)
CREATE TABLE Wishlist (
                          id INT AUTO_INCREMENT PRIMARY KEY,
                          user_id INT,
                          product_id INT,
                          added_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
                          FOREIGN KEY (user_id) REFERENCES User(id),
                          FOREIGN KEY (product_id) REFERENCES Product(id)
);

-- Product_Image 테이블 (상품 이미지 정보)
CREATE TABLE Product_Image (
                               id INT AUTO_INCREMENT PRIMARY KEY,
                               product_id INT,
                               image_url VARCHAR(255) NOT NULL,
                               created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
                               FOREIGN KEY (product_id) REFERENCES Product(id)
);

-- Coupon 테이블 (쿠폰 정보)
CREATE TABLE Coupon (
                        id INT AUTO_INCREMENT PRIMARY KEY,
                        code VARCHAR(50) NOT NULL,
                        discount_amount DECIMAL(10, 2) NOT NULL,
                        valid_from DATE NOT NULL,
                        valid_to DATE NOT NULL,
                        user_id INT,
                        FOREIGN KEY (user_id) REFERENCES User(id) ON DELETE SET NULL
);

-- Merchant 테이블 (가맹점 정보)
CREATE TABLE Merchant (
                          id INT AUTO_INCREMENT PRIMARY KEY,
                          name VARCHAR(255) NOT NULL,
                          email VARCHAR(255) NOT NULL,
                          phone VARCHAR(20),
                          address VARCHAR(255),
                          created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
                          status ENUM('ACTIVE', 'INACTIVE') NOT NULL DEFAULT 'ACTIVE'
);

-- Merchant_Product 테이블 (가맹점과 상품 간의 관계)
CREATE TABLE Merchant_Product (
                                  merchant_id INT,
                                  product_id INT,
                                  PRIMARY KEY (merchant_id, product_id),
                                  FOREIGN KEY (merchant_id) REFERENCES Merchant(id) ON DELETE CASCADE,
                                  FOREIGN KEY (product_id) REFERENCES Product(id) ON DELETE CASCADE
);

-- Settlement 테이블 (정산 내역)
CREATE TABLE Settlement (
                            id INT AUTO_INCREMENT PRIMARY KEY,
                            merchant_id INT,
                            total_sales DECIMAL(15, 2) NOT NULL,
                            settlement_amount DECIMAL(15, 2) NOT NULL,
                            settlement_date TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
                            FOREIGN KEY (merchant_id) REFERENCES Merchant(id)
);


* 정산 배치 고도화 2022.05 - 2024.11
(java, spring boot batch, jpa, queryDsl, quartz, mariaDB)
- 성능 검증을 위한 배치 테스트환경 구축하고 이를 통해 고도화에 활용할 수 있는 객관적 수치 시각화
- 수치를 바탕으로 chunk size 및 hikari connection pool 최적화
- quartz 클러스터링 + db 이중화.
- DB조회 및 쓰기 작업 시 불필요하게 발생하는 transaction과 쿼리를 제거해 성능 개선
- 비효율적인 쿼리를 튜닝하고 불필요한 인덱스 테이블 제거

* QR결제 관리자 시스템 2022.04 - 2024.11
(java, keycloak, spring boot, spring security, thymleaf, jpa, mybatis, mariaDB)
- keycloak을 이용한 sso 구현 및 세션 방식 개선.
- 가맹점 승인 처리시 동기 I/O들로 인한 지연 및 실패를 비동기 처리와 Outbox 패턴으로 개선하고, 낙관적 락으로 중복 승인 동시성 문제 해결
- 복잡한 가맹점 등록 로직을 전략 패턴 적용과 불변 DTO 적용으로 서비스와 검증 로직을 분리해 사이드 이팩트 감소 및 코드 생산성 개선
- Excel 다운로드 시 OOM문제를 스트리밍 처리 및 힙 덤프 분석으로 불필요한 객체 생성 제거로 해결
- Entity 연관관계별 전략(1:1 Fetch Join, 1:N 선택적 LAZY) 적용으로 N+1 문제 해결

* 서울페이+ 고도화 2022.01 - 2022.04
- 데브온 프레임워크환경에서 상품권 조회, 공지사항, 팝업 페이지 개발

* 농협 올원공구 프론트 신규개발 및 유지보수 2021.04 - 2022.01
- 올원뱅크앱에서 웹뷰로 제공하는 공동구매 서비스
- vue2를 이용한 spa 프론트개발